from django.forms import ModelForm

"""
        from .models import usersInfo

class createUser(ModelForm):
    class Meta:
        model = usersInfo
        fields = ['first_Name','last_Name','username','password','email','country']
"""